cmake_minimum_required(VERSION 3.13)
set(PICO_BOARD pico)
include(pico_sdk_import.cmake)
project(blink_bare)
pico_sdk_init()
add_executable(blink_bare main.c)
target_link_libraries(blink_bare pico_stdlib)
target_compile_options(blink_bare PRIVATE -O2)
pico_add_extra_outputs(blink_bare)